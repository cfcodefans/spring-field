package cfcodefans.study.spring_field.patterns

import org.slf4j.Logger
import org.slf4j.LoggerFactory

/**
 * refers to https://github.com/iluwatar/java-design-patterns/tree/master/model-view-viewmodel
 * Intent:
 *      To apply "Separation of Concerns" to separate the logic from the UI components
 *      and allow developers to work on UI without affecting the logic and vice versa.
 */
object ModelViewViewModel {
    private val log: Logger = LoggerFactory.getLogger(ModelViewViewModel::class.java)


}